# @builtbyted/eslint-plugin

ESLint rules that protect [Homeframe](https://github.com/BuiltByTed/Homeframe)
viewport, document-scroll, and input ownership boundaries.

The plugin is intended to complement the stricter rendered checks in
`@builtbyted/cli`'s `homeframe doctor` command.
