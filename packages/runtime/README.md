# @builtbyted/runtime

Viewport, safe-area, keyboard, lifecycle, installation, and runtime event
primitives for [Homeframe](https://github.com/BuiltByTed/Homeframe).

Most React applications should install `@builtbyted/react` and consume these
primitives through Homeframe hooks. See the repository README and generated
scaffold for the supported composition.
