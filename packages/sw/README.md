# @builtbyted/sw

Service-worker generation, atomic updates, offline caching, push transport, and
badging for [Homeframe](https://github.com/BuiltByTed/Homeframe).

Use it through `@builtbyted/vite` and `@builtbyted/react` unless implementing a
framework adapter. Applications must not register a competing service worker.
