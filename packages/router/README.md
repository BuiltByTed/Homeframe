# @builtbyted/router

Same-document routing, installed-iOS edge gestures, deep links, permalinks, and
scroll restoration for [Homeframe](https://github.com/BuiltByTed/Homeframe).

Compose one `HomeframeRouterProvider` around the application shell and render
routes through `RouterOutlet` inside `AppScrollView`.
